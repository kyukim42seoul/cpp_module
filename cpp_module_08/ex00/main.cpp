#include "easyfind.hpp"

int	main()
{
	std::vector<int>		iVector;
	std::list<char>			cList;
	std::deque<int>			strDeque;
	// std::vector<int>::const_iterator iter;

	iVector.

	// iter = easyfind(iVector, 3);
	std::cout << *easyfind(iVector, 3) << std::endl;
	easyfind(cList, 'd');
	easyfind(strDeque, 'h');

	return (0);
}
